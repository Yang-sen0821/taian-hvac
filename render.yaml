services:
  - type: web
    name: taian-hvac
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    # 環境變數請在 Render Dashboard 手動設定，不在此 YAML 定義
